import styled from 'styled-components';

import Presentation from 'static/images/presentation-desktop.png?external';
import Page from 'common/Page';

export const Homepage = styled(Page)`
  background: url(${Presentation});
`;